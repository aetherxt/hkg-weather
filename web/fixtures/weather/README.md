# Weather API fixtures

These are representative, reduced captures from the deployed normalized
`/api/weather/*` application API on 18 July 2026. They retain the response
envelopes, timestamps, field spelling and representative values while trimming
repeated station and forecast rows. Frontend tests use these files only and
must not call HKO or MongoDB-backed endpoints.
